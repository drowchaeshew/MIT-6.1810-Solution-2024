RISC-V assembly

# 这里的回答都看的是 user/call.asm 的汇编。

1. a2; 所有a开头的寄存器存储参数；
2. 这两个函数都没有被调用；它们在编译的时候就被inline了，最终计算结果在地址为 0x26 的指令中被使用了，即12.
3. 0x6cc
4. 将保存下一条指令的地址，即0x34
5. HE110 World. DEC(57616) == HEX(e110); ASCII('rld') == HEX(726c64); 计算机是小端序的。
    不需要改。只有当一个字的不同字节分开读的时候，字节序的不同才有意义。
6. 编译不通过，但应该是调用时会错误地读取寄存器a2的值，这个值是不定的。

